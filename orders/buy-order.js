class Order {
	// -- TICKER
	// -- SIDE: buy/sell
	// -- TYPE:
	// market order: buy now @ retail
	// *TIME sensitive / *NOT price sensitive
	// limit order: buy at set $amount$
	// *PRICE sensitive / *NOT time sensitive
	// -- PRICE: (if it's a LIMIT order)
	// -- QUANTITY: # of shares to buy
	constructor() {}
}
